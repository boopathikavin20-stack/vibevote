# Set this to the public URL of the deployed backend when frontend and backend
# are deployed separately. Leave empty when the Go server serves this frontend.
VITE_API_URL=VITE_API_URL=https://vibevote-ioiy.onrender.com/